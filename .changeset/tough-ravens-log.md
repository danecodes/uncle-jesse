---
"@danecodes/uncle-jesse-test": patch
---

Save captured Roku log session output for `RokuTestSession` log artifacts when available, with framework logger breadcrumbs as a fallback. Fix packaged Vitest fixture dependency parsing, support explicit registry seeding modes, and fall back to launch-param registry seeding when ODC is unavailable during Roku session setup.
